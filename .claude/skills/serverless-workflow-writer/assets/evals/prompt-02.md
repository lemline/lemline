Build a workflow for nightly risk processing of loan applications.
For each application, route to approve/review/reject based on score.
Call different HTTP services for each route.
At the end, emit one summary event.

